/*Arrays : it is a linear structure that stores a fixed-size seqence of elemnets of the same data typein contiguous memory locations.
each element can be accessed directly using its index, which allows for efficent retrueval and midification.*/

#include<stdio.h>

int main(){
    int arr[] = {2,4,3,5,3,5};
    int n = sizeof(arr)/sizeof(arr[0]);
    
    //printint array elements
    
    for(int i = 0; i<n ;i++){
        printf("%d ",arr[i]);
    }
    return 0;
}

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include<stdio.h>

int main(){
    //array declaration and initialization
    int arr[5] = {3,4,2,5,6};
    
    //accessing element at index 2. i.e 3rd element
    printf("%d ",arr[2]);
    
    return 0;
}
---------------------------------------------------------------------------------------------------
